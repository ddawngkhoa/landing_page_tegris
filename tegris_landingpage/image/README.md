# Image Assets for new2.html

This folder stores local images used by `new2.html`.

Required files currently wired:
- before-operating-room.svg
- after-operating-room.svg

Replace these placeholders with production images while keeping the same file names,
or update the `src` paths in `new2.html`.
